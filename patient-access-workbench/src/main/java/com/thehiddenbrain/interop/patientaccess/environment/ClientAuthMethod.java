package com.thehiddenbrain.interop.patientaccess.environment;

/** How client id and secret are presented at the token endpoint. */
public enum ClientAuthMethod {
    CLIENT_SECRET_BASIC, CLIENT_SECRET_POST
}
